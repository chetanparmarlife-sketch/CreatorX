# Enable container reuse for faster test execution
# This will reuse the same containers across test runs instead of creating new ones each time
testcontainers.reuse.enable=true

# Ryuk is test containers' resource reaper - keeps running to clean up containers (comment out to disable)
# testcontainers.ryuk.disabled=true

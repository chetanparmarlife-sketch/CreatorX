{
  "service": "creatorx-backend-staging",
  "build": {
    "command": "cd backend && ./gradlew build -x test"
  },
  "deploy": {
    "startCommand": "java -jar -Dspring.profiles.active=staging backend/creatorx-api/build/libs/creatorx-api.jar",
    "healthcheckPath": "/actuator/health",
    "restartPolicyType": "ON_FAILURE"
  },
  "environment": "staging"
}

Implemented the brand lists backend end-to-end. No frontend code changed.

Added:

V64__create_brand_lists.sql because V63 already exists
BrandListController.java
BrandListService.java
JPA entities, repositories, request/response DTOs
Service unit tests and controller integration tests
Security route matchers for /api/v1/brands/lists/**
Documentation updates in MIGRATION_FIX_LOG.md and DEPLOYMENT_ISSUES.md
Endpoints now exist:

GET /api/v1/brands/lists
POST /api/v1/brands/lists/shortlist
DELETE /api/v1/brands/lists/shortlist/{creatorId}
Checks:

git diff --check passed
Confirmed migration version does not conflict: latest is now V64
Checked new files for hardcoded UUID-style IDs
./gradlew test could not run because Java is not installed locally: Unable to locate a Java Runtime

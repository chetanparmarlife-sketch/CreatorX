/**
 * Auth configuration
 * All real auth token keys live here so screens do not hardcode the old mock login storage.
 */
export const AUTH_CONFIG = {
  tokenKey: 'access_token',
  refreshTokenKey: 'refresh_token',
  userKey: 'user_data',
};
